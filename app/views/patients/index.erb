<!-- displays a link to each patient's show page and the total number of appointments they have. -->
<% @patients.each do |patient| %>
  <%= link_to patient.name, patient_path(patient) %> --
  <%= patient.appointments.count %><br>
<% end %>
